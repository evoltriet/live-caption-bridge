# Contributor and agent instructions

Before planning or changing this repository, read `PROJECT_CONTEXT.md` completely.

- State which locked project decisions the work affects.
- Update `PROJECT_CONTEXT.md` in the same change when a decision, verification status, vendor fact, or backlog item changes.
- Never describe planned, simulated, or automated-only behavior as field-validated.
- Preserve the direct-soundboard, never-inline-with-PA, no-raw-audio-persistence, explicit-export, local-first, and offline-benchmark-gate requirements.
